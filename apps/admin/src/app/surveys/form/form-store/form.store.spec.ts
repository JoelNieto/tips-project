import { FormStore } from './form.store';

describe('FormStoreStore', () => {
  const componentStore = new FormStore();

  it('should be created', () => {
    expect(componentStore).toBeTruthy();
  });
});
